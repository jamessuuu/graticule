node_modules/
.pnpm-store/
dist/
out/
.next/
build/
# Vitest --coverage output (root-level only — NOT fixtures/coverage/,
# which is real project data, not a generated report).
/coverage/
*.log
.DS_Store
Thumbs.db
.env
.env.*
!.env.example
playwright-report/
test-results/
.turbo/
*.tsbuildinfo
/scripts/.model-cache/

# Build artifact: compiled by scripts/build-worker.mjs (esbuild), part of
# `pnpm run build` — never hand-edited, always regenerated.
/apps/web/public/worker/
