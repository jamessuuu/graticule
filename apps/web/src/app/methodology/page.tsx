export const metadata = { title: "methodology" };

export default function MethodologyPage() {
  return (
    <div>
      <h1>Methodology</h1>
      <p>Built in M6 — model choice, the WASM-vs-WebGPU batch table, and version pins.</p>
    </div>
  );
}
