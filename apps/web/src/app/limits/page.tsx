export const metadata = { title: "limits" };

export default function LimitsPage() {
  return (
    <div>
      <h1>Where this breaks</h1>
      <p>Built in M4 — the negation-blindness demo, with real live inference on editable pairs.</p>
    </div>
  );
}
