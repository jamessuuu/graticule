export const metadata = { title: "coverage" };

export default function CoveragePage() {
  return (
    <div>
      <h1>Language coverage</h1>
      <p>Built in M4 — this page will render from real fixture results, never hand-typed prose.</p>
    </div>
  );
}
